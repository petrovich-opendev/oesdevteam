"""OESDevTeam — production-grade multi-agent code generation pipeline.

Public API surface is deliberately small: most internals are accessed via the
controller (`src.feature_controller`) or the CLI entry points.
"""

__version__ = "0.1.0"
